# Backend DataPact MVP (FastAPI)

## Prérequis
- Python 3.10+

## Lancer
```bash
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app:app --reload --port 8000
```

API: http://localhost:8000/docs
